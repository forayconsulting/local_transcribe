# src/

Contains the single `transcribe` package. The `praecipio_local_transcribe.egg-info/` directory is generated build metadata and can be ignored.

## Subdirectories
| Directory | Purpose |
|-----------|---------|
| `transcribe/` | All pipeline stages and CLI entrypoint |
